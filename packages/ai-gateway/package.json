{
  "name": "@genius-x/ai-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "The only entry point for AI calls. Request building, safety filtering (input+output), token budget, provider routing, fallback library, audit logging.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@genius-x/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "vitest": "^4.1.8"
  }
}
