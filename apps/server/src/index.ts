/**
 * @genius-x/server — course engine + classroom sync + API. SKELETON (M1).
 *
 * M1 builds: XState stage machine (consumes @genius-x/course-config), Socket.IO classroom
 * sync (@genius-x/contracts ws-events), reconnect/resume from authoritative state.
 * Calls AI only through @genius-x/ai-gateway. Config via @genius-x/config.
 */
export {};
