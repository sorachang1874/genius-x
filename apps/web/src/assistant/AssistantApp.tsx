/**
 * Assistant control panel (Agent A). M3 scaffolding — shows the class's current stage and
 * unlock controls (ASSISTANT_UNLOCK / TEACHER_UNLOCK / FORCE_ADVANCE). Wired next.
 */
export function AssistantApp(): React.JSX.Element {
  return <div className="assistant-app">助教端（M3 搭建中）</div>;
}
