/**
 * Student client (Agent B). M3 scaffolding — room-code join → classroom stages (icebreak,
 * shape A-line) wired to the server over Socket.IO. Stages + socket client land next.
 */
export function StudentApp(): React.JSX.Element {
  return <div className="student-app">学生端（M3 搭建中）</div>;
}
